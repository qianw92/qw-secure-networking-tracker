# Never upload real environment values. Vercel deploys from Git so these are
# not committed anyway, but `vercel deploy` from a laptop uploads the local
# directory -- this makes sure a stray .env.local cannot ride along.
.env
.env.*
!.env.example

node_modules
dist
*.log
.DS_Store
