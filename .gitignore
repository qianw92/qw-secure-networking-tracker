# Dependencies
node_modules/

# Secrets — never commit real values.
# The negation keeps .env.example, which MUST be committed.
.env
.env.*
!.env.example

# Build output
dist/
build/
.vercel/

# Editor / OS noise
.DS_Store
*.log
